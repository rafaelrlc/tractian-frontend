export interface HeaderButtonProps {
  text: string
  isActivated: boolean
  onClick : () => void
  icon : React.ReactNode
}

export interface DashboardHeaderButton {
  text: string
  code : string
  icon : React.ReactNode
}

